# 允许的来源，* 表示允许所有，也可以指定具体域名，多个以','分隔，如 https://johnzhu.cn,https://localhost:3000
ALLOWED_ORIGINS=https://johnzhu.cn
# 目标API域名（可选），如果设置则只允许代理到该域名，提高安全性
TARGET_DOMAINS=github.com,gitlab.com,pypi.org
# 是否启用请求日志
ENABLE_LOG=true
# 请求超时时间（毫秒）
TIMEOUT=30000
# 监听端口
PORT=3000